# 参与 Suger RGB

感谢你愿意改进 Suger RGB。这个项目直接显示健康相关数据，因此改动首先要保证含义清楚、行为可预测，并避免让用户误以为它是医疗设备。

## 提交问题

请尽量提供：

- ESP32-C3 开发板型号与照片或引脚图；
- WS2812B 产品类型、灯珠数量和数据方向；
- 供电方式、接线方式以及是否使用电平转换；
- ESP-IDF 版本；
- 可以稳定复现问题的步骤；
- 已删除敏感信息的串口日志。

不要公开 Wi-Fi 密码、私有 Nightscout 地址、家庭公网地址或其他凭证。

## 本地编译

项目使用 ESP-IDF 5.4.x：

```bash
. ~/esp/esp-idf/export.sh
idf.py set-target esp32c3
idf.py build
```

当前默认目标是 ESP32-C3，WS2812B 数据脚为 `GPIO4`，BOOT 按键为 `GPIO9`。

## Pull Request 检查清单

- `idf.py build` 成功；
- 没有提交 `build/`、`sdkconfig`、密码或真实私有配置；
- 新增配网页文字时同时补充中文和英文；
- 血糖阈值、单位换算或趋势映射变化已经同步更新两个 README；
- 新增或改编第三方代码时已经核对许可证，并更新 `THIRD_PARTY_NOTICES.md`；
- 紧急阈值仍优先于普通趋势灯效；
- 页面预览不会写入 NVS，并能可靠恢复 Nightscout 自动显示。
- 正常运行时不开放 HTTP 设置页面，只有实体 BOOT 长按进入配网后才启动。

## 设计原则

- 不把灯光提示描述为医疗报警；
- 关键状态优先清楚，再考虑装饰性效果；
- 配网交互面向没有开发经验的用户；
- 默认行为应安全、可恢复，避免要求用户计时或记忆隐藏操作；
- 对电源、电流和接线限制给出明确说明。

提交代码即表示你同意按本项目的 GPL-3.0-or-later 许可证发布你的贡献。
